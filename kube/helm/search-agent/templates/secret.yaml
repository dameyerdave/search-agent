apiVersion: v1
kind: Secret
metadata:
  name: search-agent-secrets
  labels:
    {{- include "search-agent.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- toYaml .Values.secrets | nindent 2 }}
