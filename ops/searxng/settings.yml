use_default_settings: true

general:
  instance_name: "Research Search Agent"

search:
  safe_search: 0
  autocomplete: ""
  formats:
    - html
    - json

server:
  secret_key: "change-me-searxng-secret"
  limiter: false
  image_proxy: false
